var input = {
    mouseX: {
        start:0,
        end: window.innerWidth,
        current: 0
    }
}

window.addEventListener('mousemove', 
    function(){
        this.console.log('working');
    })